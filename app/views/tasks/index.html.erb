<h1>All Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to "#{task.title}", task_path(task) %> - <%= task.completed %></li>
  <% end %>
</ul>

<%= link_to "Create New Task", new_task_path %>
